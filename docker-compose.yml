version: '2'
services:
    spring-server:
        container_name: spring-server
        build:
            context: .
        image: spring-server:latest
        ports:
            - 8080:8080
        networks:
            - mindmath-cloud-network
        volumes:
            - mindmath-cloud-config-repo:/var/lib/mindmath-cloud/config-repo
        logging:
            driver: json-file
networks:
    mindmath-cloud-network:
        driver: bridge
volumes:
    mindmath-cloud-config-repo:
        external: true
